# FoodBridge Website Platform (React + Vite)

The React web platform is located in `frontend/` and `website/`.

To launch the web app:
```bash
cd frontend
npm install
npm run dev
```
Open `http://localhost:5173`.
